import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.haksikalternative",
  appName: "학식 말고 뭐 먹지?",
  webDir: "dist/public",
};

export default config;
